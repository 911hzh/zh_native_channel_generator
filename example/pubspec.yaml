name: zh_native_channel_generator_example
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  zh_native_channel:
    path: ../../zh_native_channel

dev_dependencies:
  build_runner: ^2.15.0
  zh_native_channel_generator:
    path: ..

dependency_overrides:
  zh_native_channel:
    path: ../../zh_native_channel
