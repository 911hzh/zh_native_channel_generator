name: zh_native_channel_generator
description: "Code generator for zh_native_channel."
version: 0.0.6
repository: https://github.com/hzh137940556/zh_native_channel_generator

environment:
  sdk: ^3.8.0

dependencies:
  build: ^4.0.6
  glob: ^2.1.3
  zh_native_channel: ^0.0.5

executables:
  generate_native_channel: generate_native_channel

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
